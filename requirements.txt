# HEMOSCOREAPP – Python dependencies
# Install with:  pip install -r requirements.txt
# (mirrors backend/requirements.txt — the canonical source)

# ── Web server ────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
python-multipart>=0.0.9

# ── ML model & numeric ────────────────────────
xgboost>=2.0.0
numpy>=1.26.0
joblib>=1.3.0

# ── Data handling & exports ───────────────────
pandas>=2.1.0
openpyxl>=3.1.0          # Excel read/write (.xlsx)
